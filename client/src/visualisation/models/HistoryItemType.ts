/**
 * Enum representing the types of items in the history.
 */
export enum HistoryItemType {
    /**
     * Represents a keyword in the history.
     */
    KEYWORD,
  
    /**
     * Represents an item in the history.
     */
    ITEM,
  }
  